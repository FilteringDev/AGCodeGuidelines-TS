/** @file Log method names supported by the logger. */
enum LogMethod {
    Error = 'error',
    Warn = 'warn',
    Info = 'info',
    Debug = 'debug',
    Trace = 'trace',
}

export default LogMethod;
export { LogMethod };
